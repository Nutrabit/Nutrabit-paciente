import 'package:flutter/material.dart';

class Publicidades extends StatelessWidget {
  const Publicidades({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
import 'package:flutter/widgets.dart';

class SubirArchivos extends StatelessWidget {
  const SubirArchivos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
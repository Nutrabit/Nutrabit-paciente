import 'package:flutter/widgets.dart';

class DetalleArchivo extends StatelessWidget {
  final String id;
  const DetalleArchivo({required this.id, super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
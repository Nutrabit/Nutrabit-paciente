import 'package:flutter/material.dart';

class Archivos extends StatelessWidget {
  const Archivos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
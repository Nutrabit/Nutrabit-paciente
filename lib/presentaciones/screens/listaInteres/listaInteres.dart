import 'package:flutter/material.dart';

class ListaInteres extends StatelessWidget {
  const ListaInteres({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}